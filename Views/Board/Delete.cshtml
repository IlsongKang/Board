@model BoardProject.Models.Board

@{
    ViewData["Title"] = "글 삭제";
}

<div class="container mt-4">
    <h1>글 삭제</h1>
    <div class="alert alert-danger">
        정말로 이 글을 삭제하시겠습니까?
    </div>
    
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">@Model.Title</h3>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-2">작성자</dt>
                <dd class="col-sm-10">@Model.Author</dd>
                
                <dt class="col-sm-2">작성일</dt>
                <dd class="col-sm-10">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</dd>
                
                @if (Model.UpdatedAt.HasValue)
                {
                    <dt class="col-sm-2">수정일</dt>
                    <dd class="col-sm-10">@Model.UpdatedAt.Value.ToString("yyyy-MM-dd HH:mm:ss")</dd>
                }
                
                <dt class="col-sm-2">내용</dt>
                <dd class="col-sm-10">
                    <p style="white-space: pre-wrap;">@Model.Content</p>
                </dd>
            </dl>
        </div>
        <div class="card-footer">
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="삭제" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">목록으로</a>
            </form>
        </div>
    </div>
</div> 